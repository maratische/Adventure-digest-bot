package maratische.telegram.pvddigest.repository;

//import maratische.telegram.pvddigest.model.StateMachineEntity;
//import org.springframework.data.jpa.repository.JpaRepository;
//import org.springframework.stereotype.Repository;

//@Repository
//public interface StateMachineRepository extends JpaRepository<StateMachineEntity, String> {}
//@Repository
//public interface StateMachineRepository extends JpaRepository<Post, Long> {
//
//    @Query("SELECT s FROM StateMachineContextEntity s WHERE s.state = :state")
//    List<StateMachineContextEntity> findStateMachinesByState(@Param("state") String state);
//
//    @Query("select u from Post u where u.messageId = ?1")
//    Post findByMessageId(Long messageId);
//
//    @Query("select u from Post u where u.status = ?1")
//    List<Post> findByStatus(PostStatuses status);
//
//}

